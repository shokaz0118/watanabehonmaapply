Project structure

- `frontend/` — React app (Vite)
- `backend/` — Express API
- `docs/` — documentation
- `scripts/` — helper scripts

Run `scripts\setup.ps1` to install dependencies for both frontend and backend.
